import { NavBgDirective } from './nav-bg.directive';

describe('NavBgDirective', () => {
  it('should create an instance', () => {
    const directive = new NavBgDirective();
    expect(directive).toBeTruthy();
  });
});
